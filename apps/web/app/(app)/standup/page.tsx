import { EmptyState } from "../../../components/ui/empty-state";

export default function StandupPage() {
  return <EmptyState title="Daily standups" description="Standup digests and parsing will plug into this route." />;
}
