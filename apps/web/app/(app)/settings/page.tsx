import { EmptyState } from "../../../components/ui/empty-state";

export default function SettingsPage() {
  return <EmptyState title="Settings" description="Theme, timezone, and notification preferences will live here." />;
}
