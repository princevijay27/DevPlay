import { EmptyState } from "../../../components/ui/empty-state";

export default function LeaderboardPage() {
  return <EmptyState title="Leaderboard" description="High scores will populate once game sessions are recorded." />;
}
